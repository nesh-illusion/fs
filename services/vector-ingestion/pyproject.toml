[project]
name = "lucius-embedding"
version = "0.1.0"
description = "LUCIUS Embedding platform service"
requires-python = ">=3.11"
dependencies = [
  "requests>=2.31.0",
  "azure-servicebus>=7.11.0",
  "azure-data-tables>=12.4.0",
]

[project.scripts]
lucius-embedding = "consumer.main:main"
