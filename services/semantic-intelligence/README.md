# SIS Service Skeleton

This service consumes LUCIUS directives, publishes ACK/RESULT to Service Bus reply topics, executes SIS, and posts RESULT.

Replace `execute()` in `src/consumer/__init__.py` with SIS logic.
